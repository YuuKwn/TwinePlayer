{
  "name": "twine-player",
  "version": "1.0.0",
  "description": "An isolated desktop application for playing Twine HTML games.",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "test": "node test/run-tests.js",
    "test:integration": "node test/electron-integration.js",
    "check": "node --check main.js && node --check preload.js && node --check src/storage-utils.js && node --check src/renderer.js && node --check src/shared/save-filename.js && node --check src/shared/library-history.js && node --check src/shared/game-player-helpers.js && node --check src/main/file-utils.js && node --check src/main/game-metadata.js && node --check src/main/illustrator-config.js && node --check src/main/validation.js && node --check src/main/save-service.js && node --check src/main/illustrator-service.js && node --check src/main/ipc-handlers.js && node --check src/game/player.js && node --check src/game/twine-bridge.js && node --check src/game/dev-console.js && node --check src/game/save-engine.js && node --check src/game/save-modal.js && node --check src/game/illustrator-ui.js && node --check src/game/bootstrap.js && node --check test/electron-integration.js && npm test",
    "build:win": "electron-builder --win",
    "build:win:portable": "electron-builder --win dir --publish never",
    "package:smoke": "npm run build:win:portable",
    "build:linux": "electron-builder --linux",
    "build:all": "electron-builder --win --linux"
  },
  "author": "TwinePlayer contributors",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "electron": "^30.0.0",
    "electron-builder": "^26.8.1"
  },
  "build": {
    "appId": "com.twineplayer.app",
    "productName": "Twine Player",
    "artifactName": "TwinePlayer-${version}-${os}-${arch}.${ext}",
    "files": [
      "main.js",
      "preload.js",
      "index.html",
      "game.html",
      "src/**/*",
      "package.json",
      "!src/**/*.map",
      "!test/**/*",
      "!docs/**/*",
      "!dist/**/*",
      "!builder_debug/**/*",
      "!**/*_saves/**/*",
      "!**/*_illustrations/**/*",
      "!**/*.log"
    ],
    "win": {
      "target": [
        "nsis",
        "dir"
      ]
    },
    "linux": {
      "target": "tar.gz",
      "category": "Game"
    },
    "directories": {
      "output": "dist"
    }
  }
}
