{
  "name": "twine-player",
  "version": "1.0.0",
  "description": "An isolated desktop application for playing Twine HTML games.",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "test": "node test/run-tests.js",
    "check": "node --check main.js && node --check preload.js && node --check src/main/file-utils.js && node --check src/main/validation.js && node --check src/main/save-service.js && node --check src/main/illustrator-service.js && node --check src/main/ipc-handlers.js && npm test",
    "build:win": "electron-builder --win",
    "build:linux": "electron-builder --linux",
    "build:all": "electron-builder --win --linux"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^30.0.0",
    "electron-builder": "^26.8.1"
  },
  "build": {
    "appId": "com.twineplayer.app",
    "productName": "Twine Player",
    "win": {
      "target": "nsis"
    },
    "linux": {
      "target": "tar.gz",
      "category": "Game"
    },
    "directories": {
      "output": "dist"
    }
  }
}
